rules:
  - name: workspace scripts exist
    check: test -x scripts/build.sh && test -x scripts/package.sh
  - name: workspace package script is idempotent
    check: ./scripts/package.sh && test -f dist/workspace.txt
